{
  "extends": "../tsconfig.json", // Inherit settings from the root tsconfig.json
  "compilerOptions": {
    "baseUrl": ".", // Points to the frontend directory
    "paths": {
      "@/*": ["*"]  // Alias now points directly to the frontend directory
    }
  },
  "include": ["src/**/*", "app/**/*"]
}
